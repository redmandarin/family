= simple_form_for @article do |f|
  = f.input :title
  = f.input :body, input_html: { rows: 30 }
  = f.label t("simple_form.labels.article.members")
  = f.collection_check_boxes :member_ids, Member.all, :id, :full_name
  = f.button :submit, input_html: { class: "btn btn-default btn-block" }